// initial state
const state = {
    dashboard: "dashboard",
    sales: "mdi-cart",
    orders: "mdi-buffer",
    customers: "mdi-account-group",
    customer: "mdi-account",
    addresses: "mdi-account-card-details",
    products: "mdi-package-variant",
    categories: "mdi-inbox-multiple",
    giftCards: "mdi-wallet-giftcard",
    coupons: "mdi-ticket-percent",
    cashRegisters: "mdi-cash-register",
    stores: "mdi-store",
    storePickups: "mdi-storefront",
    companies: "mdi-domain",
    taxes: "mdi-sack-percent",
    reports: "mdi-file-document-box",
    users: "mdi-account-multiple",
    barcodeScan: "mdi-barcode-scan",
    addGiftCard: "mdi-credit-card-plus",
    cashCarry: "mdi-cart-arrow-right",
    storePickup: "mdi-storefront",
    delivery: "mdi-truck-delivery",
    edit: "mdi-pencil",
    plus: "mdi-plus",
    minus: "mdi-minus",
    view: "mdi-eye",
    hold: "mdi-pause",
    delete: "mdi-delete",
    recycle: "mdi-recycle",
    notes: "mdi-note-text",
    check: "mdi-check",
    customerSearch: "mdi-account-search"
};

// getters
const getters = {
    dashboardIcon(state) {
        return state.dashboard;
    },
    salesIcon(state) {
        return state.sales;
    },
    ordersIcon(state) {
        return state.orders;
    },
    customersIcon(state) {
        return state.customers;
    },
    customerIcon(state) {
        return state.customer;
    },
    addressesIcon(state) {
        return state.addresses;
    },
    productsIcon(state) {
        return state.products;
    },
    categoriesIcon(state) {
        return state.categories;
    },
    giftCardsIcon(state) {
        return state.giftCards;
    },
    couponsIcon(state) {
        return state.coupons;
    },
    cashRegistersIcon(state) {
        return state.cashRegisters;
    },
    storesIcon(state) {
        return state.stores;
    },
    storePickupsIcon(state) {
        return state.storePickups;
    },
    companiesIcon(state) {
        return state.companies;
    },
    reportsIcon(state) {
        return state.reports;
    },
    usersIcon(state) {
        return state.users;
    },
    barcodeScanIcon(state) {
        return state.barcodeScan;
    },
    addGiftCardIcon(state) {
        return state.addGiftCard;
    },
    cashCarryIcon(state) {
        return state.cashCarry;
    },
    storePickupIcon(state) {
        return state.storePickUp;
    },
    deliveryIcon(state) {
        return state.delivery;
    },
    editIcon(state) {
        return state.edit;
    },
    plusIcon(state) {
        return state.plus;
    },
    minusIcon(state) {
        return state.minus;
    },
    holdIcon(state) {
        return state.hold;
    },
    deleteIcon(state) {
        return state.delete;
    },
    recycleIcon(state) {
        return state.recycle;
    },
    viewIcon(state) {
        return state.view;
    },
    notesIcon(state) {
        return state.notes;
    },
    checkIcon(state) {
        return state.check;
    },
    customerSearch(state) {
        return state.customerSearch;
    }
};

// actions
const actions = {};

// mutations
const mutations = {};
